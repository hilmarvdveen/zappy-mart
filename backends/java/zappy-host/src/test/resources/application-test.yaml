spring:
  datasource:
    url: jdbc:h2:mem:zappy-mart;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: zappy
    password: zappy
  jpa:
    hibernate:
      ddl-auto: create-drop

zappy:
  security:
    cookies-are-secure: false
