# The store in Java 25 on Spring Boot 4.1

Item Z6 in BACKLOG.md. The same modules as the C# backend under Maven: zappy-domain, zappy-application, zappy-adapters, zappy-host and the tests. Spring for GraphQL serves the contract, Spring Data JPA talks to PostgreSQL.

Not started. See ../../BACKLOG.md for the order of work.
