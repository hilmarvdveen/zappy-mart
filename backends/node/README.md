# The store on Node.js 24 with Apollo Server 5 (optional)

Item Z11 in BACKLOG.md, only on decision 5. Exists so the article on GraphQL with Apollo can run against a Node server of its own.

Not started. See ../../BACKLOG.md for the order of work.
