# Expected answers

Item Z2 in BACKLOG.md. One JSON answer per operation in contract/operations, with the placeholders @id, @dateTime, @token and @cursor for values that differ per run.

Not started. See ../../BACKLOG.md for the order of work.
