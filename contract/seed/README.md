# Seed data

Item Z2 in BACKLOG.md. The categories, products with stock, promotion codes with their windows and one customer with a known password that every backend loads in development and in the conformance run. The products derive from the twenty in `frontends/angular/src/assets/products.json`, with integer prices in cents and placeholder images.

Not started. See ../../BACKLOG.md for the order of work.
