mutation RegisterDuplicateEmail($input: RegisterInput!) {
  register(input: $input) {
    accessToken
    accessTokenExpiresAt
    customer {
      id
      email
    }
    errors {
      code
      field
    }
  }
}
