query ProductBySlug($slug: String!) {
  product(slug: $slug) {
    id
    name
    slug
    description
    price {
      amount
      currency
    }
    stock
    imageUrl
    category {
      id
      name
      slug
    }
  }
}
