# The conformance runner

Item Z2 in BACKLOG.md. A Node script that takes a backend URL, resets the seed, runs every operation in contract/operations in order and compares the answers with contract/expected. A backend is done when this is green.

Not started. See ../../BACKLOG.md for the order of work.
